namespace WITE::Export {

  template<LiteralList<resource> R, LiteralList<shader> S>
  struct ExportOnion {

    template<outputDescription O> auto createTarget() {
      //TODO;
    };

    template<size_t idx> auto createSource() {
    };

  };

}
