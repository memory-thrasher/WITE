#pragma once

//NOTE this file is included as if it were a header (within the namespace), because it's full of templates

template<class staticInit, class frameRender> void Flow::render(/*todo*/) {
  //TODO
}
